# NATDS is a fully static site — no runtime env vars are required.

# Leave these out of .env unless you later add a backend (e.g. Supabase,
# n8n webhook) and want to keep credentials out of source control.

# Example: opt-in analytics / tag manager ID (optional)
# VITE_GA_ID=G-XXXXXXX